apiVersion: batch/v1
kind: Job
metadata:
  name: gwgasf-glitchdata
spec:
  template:
    spec:
      containers:
      - name: gwgasf-glitchdata
        image: dfredin/gwgasf:testgwdetect
        command: ["/bin/sh", "-c"]
        args:
          - |
            mkdir -p test_data/glitch && \
            poetry run python get_segments.py && \
            poetry run python cli.py --sid 0 --eid 500 --ptype glitch
        resources:
          limits:
            memory: 32Gi
            cpu: "6000m"
          requests:
            memory: 16Gi
            cpu: "3000m"
      restartPolicy: Never
  backoffLimit: 5
